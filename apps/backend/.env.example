# Backend environment variables example
PORT=3000
API_URL=/api/v1
# Rename and set your connection string (do NOT commit secrets to git)
CONNECTION_STRING=your-mongodb-connection-string
SECRET=replace-with-a-secret
