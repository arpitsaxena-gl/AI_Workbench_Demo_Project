Create a React Native LoginScreen with two inputs (email and password) and a Sign In button. Use TypeScript, wrap the UI with the real ThemeProvider from @/theme/context, follow co-located file naming (LoginScreen.tsx), keep styles simple, and make the Sign In button trigger a mock handler. Use ES imports only, proper keyboardType for email, secureTextEntry for password, and add meaningful placeholders and accessibility labels. Avoid extra elements or smoke-test logic.